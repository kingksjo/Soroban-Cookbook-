[package]
name = "multi-sig-patterns"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "26.0.0-rc.1"

[dev-dependencies]
soroban-sdk = { version = "26.0.0-rc.1", features = ["testutils"] }
