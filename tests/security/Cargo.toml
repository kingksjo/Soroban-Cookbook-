[package]
name = "security-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
token-wrapper = { path = "../../examples/tokens/token-wrapper" }
